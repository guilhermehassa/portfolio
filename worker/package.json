{
  "name": "hassa-contact-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker que recebe o formulario de contato de hassa.dev.br e envia via API do Brevo",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
